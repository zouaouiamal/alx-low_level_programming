#include "main.h"

/**
 * _isdigit - Entry point
 *@c: int to be tested
 * prints a String
 * Return: Always 0 (Success)
 **/

int _isdigit(int c)
{
	if (c >= 48 && c <= 57)
	{
	return (1);
	}
	return (0);
}
