#include "main.h"

/**
 * mul - function that multiplate two numbers
 * @a: int type number
 * @b: int type number
 * Return: the result of multiplication
 */

int mul(int a, int b)
{
	return (a * b);
}
